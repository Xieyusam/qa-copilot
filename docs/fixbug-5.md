
我现在遇到的bug：
1.飞书文档的上传功能，点击确认导入后，弹窗没有任何响应，也没有关闭，但是已经调用了接口，前端无反馈。
2.飞书文档上传后，前端开始了轮询，这个轮询可以不要了，改为在文档列表加一个手动刷新的按钮，用于手动刷新当前知识库的文档。
3.飞书文档导入的整个流程不通，建议你使用https://xjmz.feishu.cn/wiki/YRorwd6wEi6N23kjomfc1ImOnTb这个地址来测试，然后查看接口日志排查。我希望的交互是，用户点击”确认导入后“，弹窗显示添加成功，然后立刻刷新文档列表，会出现一条飞书文档的数据，状态是同步中。然后后端会在异步任务中处理这个飞书文档的获取，清洗，以及分片，向量化入库，这后面的步骤与本地上传的保持一致，然后文档状态变成就绪。全程是无UI界面阻塞的。就是获取到飞书文档 的数据后，让它像本地文档一样的异步处理。
4.飞书文档的同步功能为手动同步数据，定时同步的功能可以移除了，后续版本再考虑加上。并且每次同步都应该全量插入，然后再把旧的数据移除，不要影响对话。手动同步是指文档列表中飞书文档类型会有一个手动同步的按钮。点击后开启异步同步的逻辑，飞书文档的状态改为同步中，等数据同步完后再变成就绪状态。
5.你要研究一下当前数据库的结构是否符合要求。