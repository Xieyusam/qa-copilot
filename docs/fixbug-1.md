目前还存在的问题：
1. 知识库分类列表支持增删改查，但是不支持设置分片策略，在编辑弹窗多了一个分片策略这是不对的。增加和编辑可以复用同一个弹窗。删除按钮只需要在了表处有按钮和二次确认弹窗即可。只有知识分类内无文档才能删除当前分类。
2. 上传知识文档-本地上传部分需要把确认上传按钮放到弹窗下方，需要与飞书链接的“确认注册”按钮的保存一样的交互。“重新选择”的按钮要更好看的样式，不要使用默认样式。
3. 飞书定时更新功能需要更细节的配置，只需要按照时间间隔更新，其他的不需要，设置更新间隔（小时）即可。同时，飞书的文档在文档列表中还需要设置一个手动同步的按钮。
4. 我需要知道飞书文档现在的更新的方式是什么，更新时会不会影响用户的使用。
5. 知识库分类文档的列表中需要展示飞书文档的类型，目前是只展示本地上传的。6.QA Copilot 对话的多模态实现是怎么样的，如何解析用户上传的文件并给到llm。请解答一下。

